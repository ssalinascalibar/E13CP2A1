<%= form_with model: @todo, local: true do |f| %>
  <%= f.label :description, "Nombre Formulario" %>
  <%= f.text_field :description %>
  <%= f.check_box :completed %>

  <%= f.submit "Crear ToDo" %>

    
<% end %>